{
  "private": true,
  "workspaces": ["ui"],
  "devDependencies": {
    "@changesets/cli": "^2.x"
  },
  "scripts": {
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  }
}
